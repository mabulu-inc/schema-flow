# Function: update_timestamp
# Trigger function that sets updated_at to now() on row update.
# Referenced by the timestamps mixin.

name: update_timestamp
language: plpgsql
returns: trigger
body: |
  BEGIN
    NEW.updated_at = now();
    RETURN NEW;
  END;
