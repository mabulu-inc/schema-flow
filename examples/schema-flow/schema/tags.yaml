# Schema for table: tags

table: tags

columns:
  - name: id
    type: serial
    primary_key: true

  - name: name
    type: varchar(50)
    unique: true

  - name: created_at
    type: timestamptz
    default: now()
